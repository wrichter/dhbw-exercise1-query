apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: query
spec:
  rules:
    - host: query-wrichter-dhbw-exercise-1.apps.osd-evalcluster.v0z2.p1.openshiftapps.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              serviceName: query
              servicePort: 4002
